# 统一行尾：仓库里一律 LF，避免 Windows 检出时出现整文件 CRLF 差异
* text=auto eol=lf

# 二进制：不要做行尾/编码转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.tgz binary

# 构建产物 / lockfile：默认折叠 diff，也不算进语言统计
lib/*.js linguist-generated=true
package-lock.json linguist-generated=true -diff
